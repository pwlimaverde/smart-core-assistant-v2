name: domain_models
description: Modelos de domínio e DTOs compartilhados do monorepo (stub estrutural).
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.12.2

dependencies:
  meta: ^1.15.0
  return_success_or_error: ^3.0.1

resolution: workspace

dev_dependencies:
  flutter_test:
    sdk: flutter

